import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  priceHeading: {
    marginLeft: 20,
  },
  graphContainer: {
    height: 200,
    alignItems: "center",
    justifyContent: "center",
  },
  graph: {
    height: 150,
    flex: 1,
    width: "100%",
  },
});
